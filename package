#!/bin/bash
set -e

PATH=$(dirname "$0")/bin:$PATH
source color

(($EUID)) && fail "Must be root"

cage=./cage
rm -rv $cage/{root,home/*}/.bash_history 2>/dev/null
tar -cvzpf image.tar.gz -C $cage "$@" .
